# Offline Map Assets

Place offline map assets here when running the CMS with local map packs.

- PMTiles or other map pack files: `packs/{version}/{file}.pmtiles`
- Map style JSON and related assets: `styles/{path}`
- Glyph PBF ranges: `fonts/{fontstack}/{range}.pbf`

The repository intentionally keeps real map data out of source control.
