@model GeoGuide.Cms.Models.Poi

@{
    ViewData["Title"] = "Sửa POI";
    var isSystemAdmin = ViewBag.IsSystemAdmin is bool value && value;
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1 class="h3 mb-0">Sửa POI</h1>
        @if (!isSystemAdmin)
        {
            <p class="text-muted mb-0">Tenant chỉ được sửa POI đang ở trạng thái chờ duyệt.</p>
        }
    </div>
    <a class="btn btn-outline-secondary" asp-action="Index">Quay lại</a>
</div>

<form asp-action="Edit" method="post" class="card shadow-sm p-4">
    <input asp-for="Id" type="hidden" />
    <partial name="_Form" model="Model" />
    <div class="mt-4 d-flex gap-2">
        <button type="submit" class="btn btn-primary">Cập nhật</button>
        <a class="btn btn-outline-secondary" asp-action="Index">Hủy</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
